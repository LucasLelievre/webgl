class Wall extends GameElement {
	
	constructor(x, y){
        super(x, y)
    }

    /**
     * Update the player
     */
    update(deltaTime){
        // Update the element
    }

    /**
     * Draw the player
     */
    draw(deltaTime) {
        // Draw your stuff
    }
}