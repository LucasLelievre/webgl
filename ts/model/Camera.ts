class Camera {

    private dir: vec3;
    private pos: vec3;

    constructor(pos: vec3, dir: vec2) {
        this.dir = dir;
        this.pos = pos;
    }

    public update(heys: string[]): void {

    }
}